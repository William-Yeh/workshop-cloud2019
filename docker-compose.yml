version: '3'
services:

  #
  # frontend
  #
  todo:
    build: ./TodoFrontend
    image: todofrontend:3.0
    ports:
      - "80:80"
    depends_on:
      - "todo-api"


  #
  # backend
  #
  todo-api:
    build: ./TodoApi
    image: todoapi:3.0
    ports:
      - "8000:80"
